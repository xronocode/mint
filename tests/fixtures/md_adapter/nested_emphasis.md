# Nested Emphasis

A paragraph with **bold word**, *italic word*, and ***bold italic phrase*** mixed together.

Another paragraph: the term **MINT** is bold; the verb *runs* is italic; the literal `code_inline` keeps its tone.
