__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
output/
tokens/
.venv/
node_modules/
dist/
*.egg-info/
.env
*.bak
*.docx.bak
*.pptx.bak
output.docx
output.pptx
output.xlsx

# OS / IDE / editor noise
.DS_Store
**/.DS_Store

# Tool telemetry
.vision-sidecar-telemetry/

# E2e generated outputs (the inputs/scripts are tracked, the outputs are not)
e2e_results/
/showcase_*.docx
/showcase_*.pptx

# Test-generated artifacts (per AGENTS.md "Output Path Convention")
tests/output/

# Agent / IDE local config
.claude/
.kilo/

# Local scratch artifacts
.last_assembled.js
.coverage
~$*.docx

# Phase-14 W3 — template versioning audit log + locally-authored sibling
# templates. The canonical templates/<name>.yaml baselines stay tracked;
# update_template-authored memo_v1.1.yaml etc. are local development
# artifacts (the real lineage lives in the GRACE manifest of produced
# docs + the per-machine _audit.jsonl).
templates/_audit.jsonl
templates/*_v*.yaml
